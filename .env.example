# ---------------------------------------------------------------------------
# TCG Emperor Store — environment template
# Copy to .env.local and fill in the values.
# ---------------------------------------------------------------------------

# Supabase — Project Settings -> API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Stripe — https://dashboard.stripe.com/test/apikeys
STRIPE_SECRET_KEY=sk_test_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Set to "true" only after activating Stripe Tax in the Stripe Dashboard
# (https://dashboard.stripe.com/settings/tax — origin address + registrations).
# Checkout sessions fail if this is on without Stripe Tax activated.
STRIPE_AUTOMATIC_TAX=false

# Public base URL of the site (no trailing slash)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
